# Local document readers

Bundled unmodified from PyPI: olefile 0.47 and xlrd 2.0.2. Licenses are included in their dist-info directories. These readers run only in the PC connection service; this directory is excluded from the public Pages artifact.

Install command: python -m pip install --target local-service/vendor olefile==0.47 xlrd==2.0.2
