# PKOS NEW 작업 일지

## 2026-09-21 GitHub Pages 재배포 준비

9월 20일 이후의 디자인 v2, 편집기, 지식맵과 로컬 연결 수정분을 기존 `legoschool/pkos` 저장소의 `main`에 반영한다. 공개 주소는 `https://legoschool.github.io/pkos/`를 유지한다.

배포 목록에서 빠진 `knowledge-graph.js`, `pkos-document.css`와 이 작업 일지를 추가했다. 서비스 워커 캐시를 `pkos-shell-20260921-new-release-v8`로 갱신했다. 생성된 시험 기록·첨부 폴더는 Git 대상에서 제외하고 로컬에 보존한다.

이번 실행에서 파일 서비스 회귀 62개, 모의 Drive 18개, 지식맵 가중치 검사가 통과했다. 실제 배포 목록으로 만든 임시 정적 사이트에서 `PKOS_NEW_화면.mjs`도 통과했다. PC·태블릿·휴대폰 크기, 지식맵, Markdown 입력·저장·재편집, 접이식 복원, 설정과 JavaScript 오류 부재를 확인했다. agent-browser의 Edge 시작은 실패하여 프로젝트의 기존 Edge 직접 검사기를 사용했다.

개인 기록, Google 계정 왕복, 실물 휴대폰은 이번 검사 대상이 아니다. 공개 웹은 정적 앱이며 PC 연결 기능은 로컬 Python 서비스가 필요하다. GitHub Actions 배포 성공과 공개 `version.json`의 커밋 일치는 업로드 후 별도로 확인한다.

## 2026-09-20 새 프로젝트 생성

기존 PKOS 커밋 `db155dc0e90d42083d5af1938df2707af877d1c6`에서 독립 작업 폴더를 만들었다. 개인 기록과 기존 실행 상태는 복사하지 않았다.

Markdown 저장, 첨부, 문서 미리보기, 로컬 파일 서비스, 이름 변경 복구, 이동·복사 복구, 녹음, 지식맵을 재사용했다. 외형은 `pkos-new.css`로 분리했다. 키보드 검색과 새 기록 단축키, 본문 건너뛰기, 저장 위치 표시는 `pkos-new.js`에 넣었다.

기본 실행 파일은 `start-pkos-new.ps1`이다. 경로를 주지 않으면 `%LOCALAPPDATA%\PKOS_NEW\시험 기록`을 연결하고 8791 포트를 쓴다. 기존 8788 서비스와 개인 기록 폴더는 건드리지 않는다.

### 확인 결과

`pkos-new.js`, `local-bridge.js` 문법 검사와 `git diff --check`가 통과했다. 모의 Drive 검사는 18개 모두 통과했다.

실제 브라우저를 8791 포트와 `%LOCALAPPDATA%\PKOS_NEW\browser-test\records`에 연결했다. 모바일 폭에서 탐색, 목록, 편집기를 열었다. 제목 `PKOS NEW 첫 검증 기록`과 한 문장 본문을 저장했고, `PKOS-index.json`과 Markdown 파일 생성을 확인했다. 개인 기록은 사용하지 않았다.

파일 서비스 회귀 60개가 전체 연속 실행에서 통과했다. 실행 시간은 35.559초였다. 두 창 동시 저장 2개는 별도로 세 번 연속 실행해 여섯 번 모두 통과했다.

기존 검사기가 Windows에서 Edge 부모만 닫아 시험용 자식 프로세스 320개가 남는 문제를 찾았다. `PC폴더연결.mjs`와 `두창동시저장.mjs`가 디버깅 포트에 속한 프로세스까지 닫도록 고쳤다. Python 검사가 Node를 강제 종료하는 경로에도 `pkos-smoke` 프로세스 정리를 넣었다. 두 Edge 엔진은 0.6초 간격으로 시작하고 연결과 저장은 겹쳐 검사한다.

PKOS NEW 화면 검사 16개가 통과했다. 1440px에서는 탐색 220px와 상단 바 시작 위치를 확인했다. 768px과 390px에서는 가로 넘침이 없었고, 390px 본문 글자는 16px 이상이었다. 본문 건너뛰기, 검색 단축키 안내, 저장 위치 상태, 별자리 표현 부재, 콘솔 오류 없음도 확인했다.

모의 Drive 검사 18개가 통과했다. 실제 Google 계정 왕복과 구분한다.

실제 Google 계정과 휴대폰 왕복, 실제 개인 자료 연결, 공개 배포는 실행하지 않았다.

## 2026-09-21 이전 상태 격리 및 Drive 파일 자동 반영

빈 목록에 예전 편집 내용과 저장 위치가 자동 복원되는 문제를 수정했다. 이전 초안은 명시적으로 불러오며, 실제 원본이 없는 기록은 보관 사본 목록으로 분리한다. 계정·Drive 루트를 바꾸면 이전 목록을 별도 보관하고 새 폴더와 섞지 않는다. 초안과 이전 폴더의 첨부 참조도 보존한다.

색인이 바뀌지 않아도 약 15초 간격으로 허용된 Drive 파일을 조회해 새 파일, Markdown 본문 수정, 폴더 이름과 이동 경로를 반영한다. 오류·권한 부족·목록 잘림을 삭제로 단정하지 않고, 조회 실패 시 업로드를 보류한다. 편집 중이거나 미전송 수정이 있는 기록은 자동 덮어쓰지 않는다. PC 폴더는 브라우저에서 직접 선택해야 하며 앱 소스 경로만으로 자동 연결되지 않는다.

모의 Drive 회귀 23개와 이전 상태·파일 반영 회귀 21개가 통과했다. 별도 로컬 시험 브라우저에서 오래된 초안의 자동 삽입 방지, 삭제 사본 분리, 색인 변경 없는 파일 추가·수정·폴더 이름 변경의 자동 반영을 확인했다. 실제 사용자의 로그인 세션과 지정한 G: 폴더 연결은 아직 확인하지 않았다.
