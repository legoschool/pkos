# PKOS NEW 작업 일지

## 2026-09-20 새 프로젝트 생성

기존 PKOS 커밋 `db155dc0e90d42083d5af1938df2707af877d1c6`에서 독립 작업 폴더를 만들었다. 개인 기록과 기존 실행 상태는 복사하지 않았다.

Markdown 저장, 첨부, 문서 미리보기, 로컬 파일 서비스, 이름 변경 복구, 이동·복사 복구, 녹음, 지식맵을 재사용했다. 외형은 `pkos-new.css`로 분리했다. 키보드 검색과 새 기록 단축키, 본문 건너뛰기, 저장 위치 표시는 `pkos-new.js`에 넣었다.

기본 실행 파일은 `start-pkos-new.ps1`이다. 경로를 주지 않으면 `%LOCALAPPDATA%\PKOS_NEW\시험 기록`을 연결하고 8791 포트를 쓴다. 기존 8788 서비스와 개인 기록 폴더는 건드리지 않는다.

### 확인 결과

`pkos-new.js`, `local-bridge.js` 문법 검사와 `git diff --check`가 통과했다. 모의 Drive 검사는 18개 모두 통과했다.

실제 브라우저를 8791 포트와 `%LOCALAPPDATA%\PKOS_NEW\browser-test\records`에 연결했다. 모바일 폭에서 탐색, 목록, 편집기를 열었다. 제목 `PKOS NEW 첫 검증 기록`과 한 문장 본문을 저장했고, `PKOS-index.json`과 Markdown 파일 생성을 확인했다. 개인 기록은 사용하지 않았다.

파일 서비스 회귀 60개가 전체 연속 실행에서 통과했다. 실행 시간은 35.559초였다. 두 창 동시 저장 2개는 별도로 세 번 연속 실행해 여섯 번 모두 통과했다.

기존 검사기가 Windows에서 Edge 부모만 닫아 시험용 자식 프로세스 320개가 남는 문제를 찾았다. `PC폴더연결.mjs`와 `두창동시저장.mjs`가 디버깅 포트에 속한 프로세스까지 닫도록 고쳤다. Python 검사가 Node를 강제 종료하는 경로에도 `pkos-smoke` 프로세스 정리를 넣었다. 두 Edge 엔진은 0.6초 간격으로 시작하고 연결과 저장은 겹쳐 검사한다.

PKOS NEW 화면 검사 16개가 통과했다. 1440px에서는 탐색 220px와 상단 바 시작 위치를 확인했다. 768px과 390px에서는 가로 넘침이 없었고, 390px 본문 글자는 16px 이상이었다. 본문 건너뛰기, 검색 단축키 안내, 저장 위치 상태, 별자리 표현 부재, 콘솔 오류 없음도 확인했다.

모의 Drive 검사 18개가 통과했다. 실제 Google 계정 왕복과 구분한다.

실제 Google 계정과 휴대폰 왕복, 실제 개인 자료 연결, 공개 배포는 실행하지 않았다.
